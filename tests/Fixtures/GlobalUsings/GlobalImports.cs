// A dedicated global-usings file: contains only `global using` directives and declares no types.
// It is never reached by the type-graph walk, so the collector must pick it up specially.
global using System.Text;
